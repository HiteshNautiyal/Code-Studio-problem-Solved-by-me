#include <bits/stdc++.h> 
int sumofDigits(int n)
{
	// Write your code here.
    if(n<10)
        return n;
    
   if(n%9==0)
       return 9;
    else
    {
        int rem = n%9;
        return rem;
    }
    
    
    
}
